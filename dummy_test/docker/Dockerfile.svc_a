# Build from repository: docker build -f dummy_test/docker/Dockerfile.svc_a -t dummy-test/svc-a:latest dummy_test
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY services/svc_a /app/service
WORKDIR /app/service
EXPOSE 8000
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
